<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="upload">

	<insert id="insert" parameterType="upload.service.UploadDto">
		insert into subject(subjectName, startTime, endTime, content, subjectImage, cost, subjecttag, t_email)
		values(#{subjectName}, #{startTime}, #{endTime}, #{content}, #{subjectImage}, #{cost}, #{subjecttag}, #{t_email})
	</insert>
	
<!-- 	<select id = "check" parameterType = "String" resultType = "int">
    SELECT COUNT(*) FROM subject WHERE subjectName = #{subjectName}
</select> -->

<select id = "check" parameterType = "String" resultType = "int">
    SELECT COUNT(*) FROM subject WHERE subjectName = #{subjectName}
</select>
	
</mapper>
